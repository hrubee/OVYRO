/**
 * Onboarding core — the shared foundation the buyer onboarding flow and the
 * admin seller-review surface both depend on (spec §4.2.4). Import from
 * `@/lib/onboarding`, not the individual modules.
 */
export * from "./status";
export * from "./schema";
export * from "./types";
